# frozen_string_literal: true

RSpec.describe Up::For::Grabs do
  it "has a version number" do
    expect(Up::For::Grabs::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
